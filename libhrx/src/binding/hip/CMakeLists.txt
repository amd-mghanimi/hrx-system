################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# libhrx/src/binding/hip/BUILD.bazel                                           #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

iree_cc_library(
  NAME
    array_lifetime
  HDRS
    "array_lifetime.h"
  SRCS
    "array_lifetime.c"
  DEPS
    iree::base
    iree::base::threading
    libhrx_defs
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
  PUBLIC
)

if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
iree_cc_library(
  NAME
    hip_dso_test_util
  HDRS
    "hip_dso_test_util.h"
  SRCS
    "hip_dso_test_util.cc"
  DEPS
    ${CMAKE_DL_LIBS}
    libhrx_defs
  TESTONLY
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
iree_cc_library(
  NAME
    printf_format
  HDRS
    "printf_format.h"
  SRCS
    "printf_format.c"
  DEPS
    iree::base
    iree::base::internal
    libhrx_defs
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
iree_cc_library(
  NAME
    hostcall_packet
  HDRS
    "hostcall_packet.h"
  DEPS
    iree::base
    iree::base::internal
    libhrx_defs
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
iree_cc_library(
  NAME
    hostcall_message
  HDRS
    "hostcall_message.h"
  SRCS
    "hostcall_message.c"
  DEPS
    ::hostcall_packet
    iree::base
    iree::base::internal::arena
    libhrx_defs
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
iree_cc_library(
  NAME
    blocking_printf_protocol
  HDRS
    "blocking_printf_protocol.h"
  SRCS
    "blocking_printf_protocol.c"
  DEPS
    ::hostcall_message
    ::printf_format
    iree::base
    iree::hal
    libhrx_defs
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
iree_cc_library(
  NAME
    blocking_printf_provider
  HDRS
    "blocking_printf_provider.h"
  SRCS
    "blocking_printf_provider.c"
  DEPS
    ::blocking_printf_protocol
    iree::base
    iree::hal
    libhrx_defs
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
  PUBLIC
)
endif()

iree_cc_library(
  NAME
    execution_resource
  HDRS
    "api.h"
    "execution_resource.h"
  SRCS
    "execution_resource.c"
  DEPS
    iree::base
    iree::hal
    libhrx::src::binding::common
    libhrx::src::binding::common::execution_resource
    libhrx_defs
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../libhrx>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../libhrx>"
  PUBLIC
)

iree_cc_library(
  NAME
    launch_params
  HDRS
    "api.h"
    "launch_params.h"
  SRCS
    "launch_params.c"
  DEPS
    iree::base
    iree::hal
    libhrx::src::binding::common
    libhrx_defs
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../libhrx>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../libhrx>"
  PUBLIC
)

iree_cc_library(
  NAME
    stream_value
  HDRS
    "stream_value.h"
  SRCS
    "stream_value.c"
  DEPS
    iree::base
    iree::hal
    libhrx_defs
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
  PUBLIC
)

iree_cc_library(
  NAME
    handle_registry
  HDRS
    "handle_registry.h"
  SRCS
    "handle_registry.c"
  DEPS
    iree::base
    iree::base::threading
    libhrx_defs
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
  PUBLIC
)

iree_cc_library(
  NAME
    function_handle
  HDRS
    "function_handle.h"
  SRCS
    "function_handle.c"
  DEPS
    ::handle_registry
    iree::base
    iree::base::threading
    libhrx::src::binding::common
    libhrx_defs
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
  PUBLIC
)

iree_cc_library(
  NAME
    execution_resource_descriptor
  HDRS
    "execution_resource_descriptor.h"
  SRCS
    "execution_resource_descriptor.c"
  DEPS
    ::execution_resource
    ::handle_registry
    iree::base
    iree::base::threading
    iree::hal
    libhrx::src::binding::common
    libhrx::src::binding::common::execution_resource
    libhrx_defs
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../libhrx>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../libhrx>"
  PUBLIC
)

iree_cc_library(
  NAME
    execution_context
  HDRS
    "execution_context.h"
  SRCS
    "execution_context.c"
  DEPS
    ::execution_resource
    ::execution_resource_descriptor
    ::stream
    iree::base
    iree::base::threading
    iree::hal
    libhrx::src::binding::common
    libhrx_defs
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../libhrx>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../libhrx>"
  PUBLIC
)

if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
iree_cc_library(
  NAME
    peer
  HDRS
    "peer.h"
  SRCS
    "peer.c"
  DEPS
    iree::base
    iree::hal
    libhrx_defs
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
  PUBLIC
)
endif()

iree_cc_library(
  NAME
    stream
  HDRS
    "api.h"
    "stream.h"
  SRCS
    "stream.c"
  DEPS
    ::handle_registry
    iree::base
    iree::base::internal
    iree::base::threading
    iree::hal
    libhrx::src::binding::common
    libhrx_defs
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../libhrx>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../libhrx>"
  PUBLIC
)

if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
iree_cc_library(
  NAME
    spirv_linker_compiler
  HDRS
    "spirv_linker_compiler.h"
  SRCS
    "spirv_linker_compiler.c"
  DEPS
    iree::base
    iree::base::internal::dynamic_library
    libhrx_defs
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
  PUBLIC
)
endif()

iree_cc_library(
  NAME
    error_state
  HDRS
    "api.h"
    "error_state.h"
    "error_state_test_util.h"
  SRCS
    "error_state.c"
  DEPS
    iree::base
    iree::base::core_headers
    iree::base::internal
    libhrx_defs
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
  PUBLIC
)

iree_cc_library(
  NAME
    status_conversion
  HDRS
    "api.h"
    "status_conversion.h"
  SRCS
    "status_conversion.c"
  DEPS
    iree::base
    libhrx_defs
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
  PUBLIC
)

iree_cc_library(
  NAME
    device_properties
  HDRS
    "device_properties.h"
  SRCS
    "device_properties.c"
  DEPS
    libhrx::src::binding::common::amdgpu_architecture
    libhrx_defs
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
  PUBLIC
)

iree_cc_library(
  NAME
    staged_copy
  HDRS
    "staged_copy.h"
  SRCS
    "staged_copy.c"
  DEPS
    iree::base
    iree::hal
    libhrx::src::binding::common
    libhrx_defs
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
  PUBLIC
)

if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
iree_cc_library(
  NAME
    amdhip64
  COPTS
    "-DIREE_HAL_STREAMING_HIP_EXPORTS"
    "-Wno-unused-variable"
  HDRS
    "api.h"
    "binding_internal.h"
    "error_state.h"
    "vmm.h"
  SRCS
    "abi_stubs.c"
    "api.c"
    "api_metadata.c"
    "library.c"
    "linker.c"
    "vmm.c"
  DEPS
    ::array_lifetime
    ::blocking_printf_provider
    ::device_properties
    ::error_state
    ::execution_context
    ::execution_resource
    ::execution_resource_descriptor
    ::function_handle
    ::handle_registry
    ::launch_params
    ::peer
    ::spirv_linker_compiler
    ::staged_copy
    ::status_conversion
    ::stream
    ::stream_value
    iree::base
    iree::base::core_headers
    iree::base::internal
    iree::base::threading
    iree::hal
    libhrx::src::binding::common
    libhrx_defs
  SHARED
  LINKOPTS
    "-Wl,--undefined-version"
    "-Wl,--version-script=${CMAKE_CURRENT_SOURCE_DIR}/amdhip64.map"
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../libhrx>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../libhrx>"
  PUBLIC
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
iree_cc_library(
  NAME
    amdhip64_array_lease_test
  COPTS
    "-DHRX_ENABLE_ARRAY_LEASE_TEST_HOOK"
    "-DIREE_HAL_STREAMING_HIP_EXPORTS"
    "-Wno-unused-variable"
  HDRS
    "api.h"
    "binding_internal.h"
    "vmm.h"
  SRCS
    "abi_stubs.c"
    "api.c"
    "api_metadata.c"
    "array_lease_test_hook.c"
    "library.c"
    "linker.c"
    "vmm.c"
  DEPS
    ::array_lifetime
    ::blocking_printf_provider
    ::device_properties
    ::error_state
    ::execution_context
    ::execution_resource
    ::execution_resource_descriptor
    ::function_handle
    ::handle_registry
    ::launch_params
    ::peer
    ::spirv_linker_compiler
    ::staged_copy
    ::status_conversion
    ::stream
    ::stream_value
    iree::base
    iree::base::core_headers
    iree::base::internal
    iree::base::threading
    iree::hal
    libhrx::src::binding::common
    libhrx_defs
  TESTONLY
  SHARED
  LINKOPTS
    "-Wl,--undefined-version"
    "-Wl,--version-script=${CMAKE_CURRENT_SOURCE_DIR}/amdhip64.map"
  INCLUDES
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../..>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../../include>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../../libhrx>"
    "$<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}/../../libhrx>"
  PUBLIC
)
endif()

iree_cc_test(
  NAME
    array_lifetime_test
  SRCS
    "array_lifetime_test.cc"
  DEPS
    ::array_lifetime
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
)

iree_cc_test(
  NAME
    execution_resource_test
  SRCS
    "execution_resource_test.cc"
  DEPS
    ::execution_resource
    iree::base
    iree::hal
    iree::hal::testing::mock_device
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx::src::binding::common
    libhrx_defs
)

iree_cc_test(
  NAME
    execution_resource_descriptor_test
  SRCS
    "execution_resource_descriptor_test.cc"
  DEPS
    ::execution_resource
    ::execution_resource_descriptor
    iree::base
    iree::hal
    iree::hal::testing::mock_device
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx::src::binding::common
    libhrx_defs
)

if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
iree_cc_test(
  NAME
    peer_test
  SRCS
    "peer_test.cc"
  DEPS
    ::peer
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
)
endif()

iree_cc_test(
  NAME
    stream_value_test
  SRCS
    "stream_value_test.cc"
  DEPS
    ::stream_value
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
)

iree_cc_test(
  NAME
    handle_registry_test
  SRCS
    "handle_registry_test.cc"
  DEPS
    ::handle_registry
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
)

iree_cc_test(
  NAME
    function_handle_test
  SRCS
    "function_handle_test.cc"
  DEPS
    ::function_handle
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx::src::binding::common
    libhrx_defs
)

if(TARGET libhrx_src_binding_hip_amdhip64)
set_target_properties(libhrx_src_binding_hip_amdhip64 PROPERTIES
  OUTPUT_NAME amdhip64
  VERSION 7.9999.0
  SOVERSION 7
  POSITION_INDEPENDENT_CODE ON
  INSTALL_RPATH "$ORIGIN"
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
iree_cc_test(
  NAME
    hip_api_name_test
  SRCS
    "hip_api_name_test.cc"
  DATA
    "libhrx::src::binding::hip::amdhip64"
  DEPS
    ${CMAKE_DL_LIBS}
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
  ENV
    "HRX_TEST_LIBAMDHIP64={{$<TARGET_FILE:libhrx::src::binding::hip::amdhip64>}}"
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    hip_array_copy_spt_api_test
  SRCS
    "api.h"
    "hip_array_copy_spt_api_test.cc"
  DATA
    "libhrx::src::binding::hip::amdhip64_array_lease_test"
  DEPS
    ${CMAKE_DL_LIBS}
    ::hip_dso_test_util
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
  ENV
    "HRX_TEST_LIBAMDHIP64={{$<TARGET_FILE:libhrx::src::binding::hip::amdhip64_array_lease_test>}}"
  LABELS
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    gpu-device
  SANITIZER_SUPPRESSIONS
    lsan
    hsa
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    hip_device_attribute_validation_api_test
  SRCS
    "api.h"
    "hip_device_attribute_validation_api_test.cc"
    "hip_device_attribute_validation_test_shim.c"
    "hip_device_attribute_validation_test_shim.h"
  DATA
    "libhrx::src::binding::hip::amdhip64"
  DEPS
    ${CMAKE_DL_LIBS}
    ::hip_dso_test_util
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
  ENV
    "HRX_TEST_LIBAMDHIP64={{$<TARGET_FILE:libhrx::src::binding::hip::amdhip64>}}"
  LABELS
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    gpu-device
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    hip_unified_addressing_attribute_api_test
  SRCS
    "api.h"
    "hip_unified_addressing_attribute_api_test.cc"
  DATA
    "libhrx::src::binding::hip::amdhip64"
  DEPS
    ${CMAKE_DL_LIBS}
    ::hip_dso_test_util
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
  ENV
    "HRX_TEST_LIBAMDHIP64={{$<TARGET_FILE:libhrx::src::binding::hip::amdhip64>}}"
  LABELS
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    gpu-device
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    hip_device_attribute_no_device_api_test
  SRCS
    "api.h"
    "hip_device_attribute_no_device_api_test.cc"
    "hip_device_attribute_validation_test_shim.c"
    "hip_device_attribute_validation_test_shim.h"
  DATA
    "libhrx::src::binding::hip::amdhip64"
  DEPS
    ${CMAKE_DL_LIBS}
    ::hip_dso_test_util
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
  ENV
    "HRX_TEST_LIBAMDHIP64={{$<TARGET_FILE:libhrx::src::binding::hip::amdhip64>}}"
    "ROCR_VISIBLE_DEVICES=-1"
  LABELS
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    gpu-device
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    hip_execution_resource_api_test
  SRCS
    "hip_execution_resource_api_test.cc"
  DATA
    "libhrx::src::binding::hip::amdhip64"
  DEPS
    ${CMAKE_DL_LIBS}
    ::execution_resource
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
  ENV
    "HRX_TEST_LIBAMDHIP64={{$<TARGET_FILE:libhrx::src::binding::hip::amdhip64>}}"
  LABELS
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    gpu-device
  SANITIZER_SUPPRESSIONS
    lsan
    hsa
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    hip_device_contracts_api_test
  SRCS
    "api.h"
    "hip_device_contracts_api_test.cc"
  DATA
    "libhrx::src::binding::hip::amdhip64"
  DEPS
    ::hip_dso_test_util
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
  ENV
    "HRX_TEST_LIBAMDHIP64={{$<TARGET_FILE:libhrx::src::binding::hip::amdhip64>}}"
  LABELS
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    gpu-device
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    hip_stream_value_api_test
  SRCS
    "hip_stream_value_api_test.cc"
  DATA
    "libhrx::src::binding::hip::amdhip64"
  DEPS
    ${CMAKE_DL_LIBS}
    ::launch_params
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
  ENV
    "HRX_TEST_LIBAMDHIP64={{$<TARGET_FILE:libhrx::src::binding::hip::amdhip64>}}"
  LABELS
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    gpu-device
  SANITIZER_SUPPRESSIONS
    lsan
    hsa
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    hip_graph_api_test
  SRCS
    "api.h"
    "hip_graph_api_test.cc"
  DATA
    "libhrx::src::binding::hip::amdhip64"
  DEPS
    ::hip_dso_test_util
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
  ENV
    "HRX_TEST_LIBAMDHIP64={{$<TARGET_FILE:libhrx::src::binding::hip::amdhip64>}}"
  LABELS
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    gpu-device
  SANITIZER_SUPPRESSIONS
    lsan
    hsa
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux" AND IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    hip_get_proc_address_test
  SRCS
    "api.h"
    "hip_get_proc_address_test.cc"
  DATA
    "libhrx::src::binding::hip::amdhip64"
  DEPS
    ${CMAKE_DL_LIBS}
    ::hip_dso_test_util
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
  ENV
    "HRX_TEST_LIBAMDHIP64={{$<TARGET_FILE:libhrx::src::binding::hip::amdhip64>}}"
  LABELS
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    gpu-device
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
iree_cc_test(
  NAME
    hip_memory_pool_api_test
  SRCS
    "hip_memory_pool_api_test.cc"
  DATA
    "libhrx::src::binding::hip::amdhip64"
  DEPS
    ${CMAKE_DL_LIBS}
    ::launch_params
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
  ENV
    "HRX_TEST_LIBAMDHIP64={{$<TARGET_FILE:libhrx::src::binding::hip::amdhip64>}}"
  SANITIZER_SUPPRESSIONS
    lsan
    hsa
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
iree_cc_test(
  NAME
    hostcall_message_test
  SRCS
    "hostcall_message_test.cc"
  DEPS
    ::hostcall_message
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
iree_cc_test(
  NAME
    blocking_printf_protocol_test
  SRCS
    "blocking_printf_protocol_test.cc"
  DEPS
    ::blocking_printf_protocol
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
)
endif()

if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
iree_cc_test(
  NAME
    blocking_printf_provider_test
  SRCS
    "blocking_printf_provider_test.cc"
  DEPS
    ::blocking_printf_protocol
    ::blocking_printf_provider
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
)
endif()

iree_cc_test(
  NAME
    launch_params_test
  SRCS
    "launch_params_test.cc"
  DEPS
    ::launch_params
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
)

iree_cc_test(
  NAME
    error_state_test
  SRCS
    "error_state_test.cc"
  DEPS
    ::error_state
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
)

iree_cc_test(
  NAME
    status_conversion_test
  SRCS
    "status_conversion_test.cc"
  DEPS
    ::error_state
    ::status_conversion
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
)

iree_cc_test(
  NAME
    device_properties_test
  SRCS
    "device_properties_test.cc"
  DEPS
    ::device_properties
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
)

if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
iree_cc_test(
  NAME
    printf_format_test
  SRCS
    "printf_format_test.cc"
  DEPS
    ::printf_format
    iree::testing::gtest
    iree::testing::gtest_main
    libhrx_defs
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###

# Point HIP DSO tests at the shared object built in this tree so they dlopen()
# the exact artifact under test, and make sure that artifact is built first.
# The build-tree path is only valid for the in-tree CTest run.
if(TARGET libhrx_src_binding_hip_hip_dso_test_util)
  target_compile_definitions(libhrx_src_binding_hip_hip_dso_test_util
    PRIVATE
      "HRX_TEST_LIBAMDHIP64_PATH=\"$<TARGET_FILE:libhrx_src_binding_hip_amdhip64>\""
  )
  add_dependencies(libhrx_src_binding_hip_hip_dso_test_util
    libhrx_src_binding_hip_amdhip64
  )
endif()
foreach(_hrx_hip_dso_test
    libhrx_src_binding_hip_hip_api_name_test
    libhrx_src_binding_hip_hip_device_contracts_api_test
    libhrx_src_binding_hip_hip_device_attribute_no_device_api_test
    libhrx_src_binding_hip_hip_device_attribute_validation_api_test
    libhrx_src_binding_hip_hip_get_proc_address_test
    libhrx_src_binding_hip_hip_launch_validation_api_test
    libhrx_src_binding_hip_hip_unified_addressing_attribute_api_test)
  if(TARGET ${_hrx_hip_dso_test})
    target_compile_definitions(${_hrx_hip_dso_test}
      PRIVATE
        "HRX_TEST_LIBAMDHIP64_PATH=\"$<TARGET_FILE:libhrx_src_binding_hip_amdhip64>\""
    )
    add_dependencies(${_hrx_hip_dso_test}
      libhrx_src_binding_hip_amdhip64
    )
  endif()
endforeach()

# The array-copy suite requires its test-only lease observer even when invoked
# directly without the CTest-provided HRX_TEST_LIBAMDHIP64 environment variable.
if(TARGET libhrx_src_binding_hip_hip_array_copy_spt_api_test)
  target_compile_definitions(
    libhrx_src_binding_hip_hip_array_copy_spt_api_test
    PRIVATE
      "HRX_TEST_LIBAMDHIP64_PATH=\"$<TARGET_FILE:libhrx_src_binding_hip_amdhip64_array_lease_test>\""
  )
  add_dependencies(
    libhrx_src_binding_hip_hip_array_copy_spt_api_test
    libhrx_src_binding_hip_amdhip64_array_lease_test
  )
endif()

# The symbol version script and --undefined-version are applied through the
# generated LINKOPTS above (from linkopts in BUILD.bazel). Keep them out of
# LINK_FLAGS here so the version script is not applied twice.
if(TARGET libhrx_src_binding_hip_amdhip64)
  install(TARGETS libhrx_src_binding_hip_amdhip64
    LIBRARY DESTINATION "${CMAKE_INSTALL_LIBDIR}"
      COMPONENT "${HRX_PUBLIC_DIST_COMPONENT}"
      NAMELINK_COMPONENT "${HRX_PUBLIC_DIST_COMPONENT}"
    ARCHIVE DESTINATION "${CMAKE_INSTALL_LIBDIR}"
      COMPONENT "${HRX_PUBLIC_DIST_COMPONENT}"
    RUNTIME DESTINATION "${CMAKE_INSTALL_BINDIR}"
      COMPONENT "${HRX_PUBLIC_DIST_COMPONENT}")
endif()
